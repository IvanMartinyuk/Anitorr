name: nyaa
description: A small, typed client for searching and parsing nyaa.si.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.1

dependencies:
  html: ^0.15.6
  http: ^1.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
